# YOLOv5 Tong数据集项目完成总结（最新）

## 🎯 项目目标
- 将 Labelme 标注的 Error/Success 两类数据转换为 YOLOv5 可训练格式
- 解决数据量极少与目标形状“细长条贯穿”带来的训练困难
- 完成多轮训练、验证与推理，并输出可复现实验配置

## ✅ 已完成工作（按时间线）
1) 数据转换
- 原始数据：`/Users/zhanghaining/2022/dataset/DataSet/tong/data`
- 转换输出：`/Users/zhanghaining/2022/dataset/DataSet/tong/yolo_dataset`
- 转换脚本：`convert_labelme_to_yolo.py`（80/20 划分，YOLO格式标签）

2) 训练配置
- 初始配置：`data/tong.yaml`
- 针对长条目标优化的超参：`data/hyp_tong_long_targets.yaml`（关闭会破坏长条形状的增强，放宽 IoU 与 anchor 匹配阈值）
- 扩充数据集配置：`data/tong_expanded.yaml`

3) 关键修复
- 修复 PyTorch AMP 弃用接口：将 `torch.cuda.amp.autocast(...)` 更换为 `torch.amp.autocast('cuda'| 'cpu')`
  - 涉及文件：`train.py`、`segment/train.py`、`utils/autobatch.py`、`classify/val.py`

4) 针对数据特点的策略
- 小数据集：采用冻结 backbone 前 20 层，仅微调高层特征；降低/关闭易破坏几何形态的增强
- 细长条目标：优化超参以提升召回；建议使用 YOLOv5 内置自动锚框检查（默认启用）
- 分辨率：以 320×320 进行更快更稳的微调

5) 数据扩充（复制倍增）
- 新数据集：`/Users/zhanghaining/2022/dataset/DataSet/tong/yolo_dataset_expanded`
- 方法：每个文件复制 5 份（images 与 labels 同步），显著增加训练样本
- 配置：`data/tong_expanded.yaml`

## 🧪 训练记录（主要节点）
- 基线小集（640）早期实验：`runs/train/exp7`（mAP 较低，数据过少）
- 学习率与策略优化：`runs/train/exp8`（指标显著提升，Error 类表现较好）
- 扩充数据训练：`runs/train/tong_expanded_training/`（使用 320、长条目标超参、冻结前 20 层）
  - 权重：`best.pt`、`last.pt`
- 基于上一轮最后权重继续微调（finetune）：`runs/train/tong_expanded_finetune/`
  - 起点权重：`runs/train/tong_expanded_training/weights/last.pt`
  - 权重：`best.pt`、`last.pt`

注：在 Mac/CPU 环境下训练时间短、数据极少，指标主要受数据量与目标形状分布影响；扩充与微调已显著改善稳定性与召回。

## 🔍 推理与验证
- 指定文件夹推理：
  - 权重：`runs/train/tong_expanded_finetune/weights/best.pt`
  - 源：`/Users/zhanghaining/2022/dataset/DataSet/tong/test`
  - 输出：`runs/detect/exp*`（含绘制框结果与 `labels/` 文本输出）

## ⚙️ 训练/验证命令（可复现）
- 使用扩充数据集训练：
```bash
python train.py \
  --data data/tong_expanded.yaml \
  --weights yolov5s.pt \
  --epochs 1000 \
  --batch-size 4 \
  --img 320 \
  --hyp data/hyp_tong_long_targets.yaml \
  --freeze 0 20 \
  --patience 200 \
  --name tong_expanded_training
```

- 基于上一轮最后权重 finetune：
```bash
python train.py \
  --data data/tong_expanded.yaml \
  --weights runs/train/tong_expanded_training/weights/last.pt \
  --epochs 1000 \
  --batch-size 4 \
  --img 320 \
  --hyp data/hyp_tong_long_targets.yaml \
  --freeze 0 20 \
  --patience 200 \
  --name tong_expanded_finetune
```

- 指定文件夹推理：
```bash
python detect.py \
  --weights runs/train/tong_expanded_finetune/weights/best.pt \
  --source /Users/zhanghaining/2022/dataset/DataSet/tong/test \
  --img 320 --conf 0.25 --save-txt --save-conf
```

## 🧩 关于 Warning：corrupt JPEG restored and saved
- 含义：检测到 JPEG 轻微损坏，已自动修复并覆盖保存
- 影响：对训练几乎可忽略；可能轻微增加数据读取时延
- 建议：可继续正常训练

## 📌 文件与路径总览（关键）
- 原始数据：`/Users/zhanghaining/2022/dataset/DataSet/tong/data`
- 训练数据（原）：`/Users/zhanghaining/2022/dataset/DataSet/tong/yolo_dataset`
- 训练数据（扩充）：`/Users/zhanghaining/2022/dataset/DataSet/tong/yolo_dataset_expanded`
- 配置文件：
  - `data/tong_expanded.yaml`（扩充版数据路径）
  - `data/hyp_tong_long_targets.yaml`（长条目标超参）
- 训练输出：
  - `runs/train/tong_expanded_training/`
  - `runs/train/tong_expanded_finetune/`
- 推理输出：`runs/detect/exp*`

## 📊 当前瓶颈与改进方向
- 数据多样性仍不足（尽管复制扩充能稳定优化训练，但不能替代真实多样样本）
- Success 类样本相对稀缺，建议增加真实样本与多尺度、不同背景样本
- 可尝试更大模型（`yolov5m/l`）或更长训练（配合早停/降低学习率）
- 结合自动锚框检查结果，必要时手动设定更“细长”的先验 anchor

---
**环境**：Anaconda 环境 `yolov5`（CPU）  
**框架**：PyTorch 2.4.1（AMP 接口已修复）  
**模型**：YOLOv5s（冻结前 20 层进行微调）  
**分辨率**：320×320  
**最新更新日期**：2024-08-28
